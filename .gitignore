# Build artifacts
/nh-helper
/nh-helper.exe
/bin/

# Local secrets — never commit
/config.json

# Runtime state: cache + glossary live database, plus snapshots
/db/

# Legacy root-level DB / WAL files (auto-migrated into db/ on first launch)
/nh-helper.db
/nh-helper.db-wal
/nh-helper.db-shm

# Auto-generated reference manual (regenerated on first launch)
/nh-helper-手册.pdf

# Debug logs (now under log/, plus legacy root paths just in case)
/log/
/nh-helper.raw.log
/nh-helper.translate.log

# macOS / IDE
.DS_Store
.vscode/
.idea/
*.swp
*.swo
